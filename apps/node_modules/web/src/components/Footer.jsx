import { Moon, Github, Instagram, Linkedin, ArrowUp } from 'lucide-react';
import { motion } from 'framer-motion';

export default function Footer() {
    const scrollToTop = () => {
        window.scrollTo({ top: 0, behavior: 'smooth' });
    };

    return (
        <footer className="mt-12 py-10 border-t border-white/5 flex flex-col md:flex-row items-center justify-between gap-8 relative">
            <div className="flex flex-col items-center md:items-start gap-3 text-gray-500 text-sm">
                <div className="flex items-center gap-2 group">
                    <Moon className="w-4 h-4 text-emerald-500 fill-emerald-500 group-hover:rotate-12 transition-transform duration-300" />
                    <span className="hover:text-emerald-400/80 transition-colors">
                        © {new Date().getFullYear()} Ramadan Companion by Qais. Built for spiritual growth.
                    </span>
                </div>
            </div>

            <div className="flex items-center gap-6">
                {[
                    { icon: Github, href: "https://github.com/asyabdqais", label: "GITHUB", color: "hover:text-emerald-400" },
                    { icon: Instagram, href: "https://instagram.com/asyabdqais", label: "INSTAGRAM", color: "hover:text-pink-500" },
                    { icon: Linkedin, href: "https://linkedin.com/in/Asyajj abdul qois", label: "LINKEDIN", color: "hover:text-blue-500" }
                ].map((social) => (
                    <motion.a
                        key={social.label}
                        href={social.href}
                        target="_blank"
                        rel="noopener noreferrer"
                        whileHover={{ y: -3, scale: 1.05 }}
                        whileTap={{ scale: 0.95 }}
                        className={`flex items-center gap-1.5 text-[10px] font-bold tracking-[0.2em] text-gray-500 ${social.color} transition-colors group`}
                    >
                        <social.icon className="w-4 h-4" />
                        <span className="hidden sm:inline">{social.label}</span>
                    </motion.a>
                ))}
            </div>

            {/* Scroll to top button */}
            <motion.button
                onClick={scrollToTop}
                whileHover={{ scale: 1.1 }}
                whileTap={{ scale: 0.9 }}
                className="md:absolute md:-top-5 md:left-1/2 md:-translate-x-1/2 bg-[#122A2F] border border-emerald-500/30 p-2.5 rounded-full text-emerald-400 hover:bg-emerald-500 hover:text-white transition-all shadow-lg cursor-pointer"
                title="Scroll to Top"
            >
                <ArrowUp className="w-4 h-4" />
            </motion.button>
        </footer>
    );
}
